export interface JobInfo {
    company: string
    title: string
    description: string
  }
  export interface CandidateInfo {
    name: string
    email: string
    experience: number
  }
  export interface SessionResponse {
    session_id: string
  }